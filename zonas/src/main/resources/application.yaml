server:
  port: 3003

spring:
  application:
    name: zonas-api

  datasource:
    url: jdbc:postgresql://localhost:5432/zonas_db
    username: postgres
    password: admin123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true